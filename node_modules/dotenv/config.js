(function () {
  const apiDomain = ' https://cev7cu4fph.execute-api.us-east-1.amazonaws.com/dev/hello'; // Reemplaza 'URL_de_tu_API_aquí' con el URL de tu API

  require('./lib/main').config(
    Object.assign(
      {},
      require('./lib/env-options'),
      require('./lib/cli-options')(process.argv),
      { API_DOMAIN: apiDomain } // Agrega el dominio de tu API como una nueva opción de configuración
    )
  )
})()
